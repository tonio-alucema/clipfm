/**
 * Which room you land in.
 *
 * Phase 0 built for one room but keyed everything by slug, so a second is
 * cheap. Until there is one, the front door leads here rather than to a list
 * of one thing.
 */
export const DEFAULT_ROOM_SLUG = 'main';
